import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorLabel: {
    color: 'red'
  },
  normalLabel: {
    color: 'grey',
  },
  normalOnBlurLabel: {
    fontSize: 16,
  },
  normalOnFocusLabel: {
    fontSize: 14,
  },
});

export default styles;