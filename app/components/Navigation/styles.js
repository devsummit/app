import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  iconDefault: {
    color: 'white',
    opacity: 0.5,
    fontSize: 22,
  },
  iconActive: {
    color: 'white',
    opacity: 1,
    fontSize: 22,
  }
});

export default styles;