import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  ticketId: {
    marginBottom: 12
  },
  ticketCode: {
    fontSize: 20,
    marginTop: 12,
    fontWeight: 'bold'
  }
});
